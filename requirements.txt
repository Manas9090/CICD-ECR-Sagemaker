# requirements.txt

boto3==1.34.131       # AWS SDK for Python
sagemaker==2.215.2    # SageMaker Python SDK
pandas==2.2.2         # Data processing
numpy==1.26.4         # Numerical computing
scikit-learn==1.5.0   # ML utilities (if your model uses sklearn)
joblib==1.4.2         # For loading/saving .pkl models
flask==3.0.3          # If you need an API wrapper
gunicorn==22.0.0      # Production WSGI server (optional, for deployment in Docker)
